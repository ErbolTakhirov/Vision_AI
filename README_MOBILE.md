# 📱 Vision AI Mobile App (Android/iOS)

Это нативное мобильное приложение для Vision AI. Оно работает быстрее, чем веб-версия, использует нативную камеру и микрофон, и его можно загрузить в **Google Play Market**.

## 🌍 Языки / Languages
Приложение автоматически определяет язык телефона. Поддерживаются:
*   🇷🇺 Русский
*   🇺🇸 English
*   🇰🇬 Кыргызча

## 🚀 Настройка и Запуск (для тебя)

### Шаг 1. Установи Flutter
Если у тебя еще нет Flutter, скачай и установи его: https://docs.flutter.dev/get-started/install/windows

### Шаг 2. Создай проект
Открой терминал в этой папке (`tg_mini_app`) и запусти:
```powershell
flutter create vision_mobile --org com.vision.ai
```

### Шаг 3. Скопируй мои файлы
Я уже создал для тебя готовый код в папке `mobile_src` (которую я сейчас создам). Тебе нужно просто заменить файлы в созданном проекте:

1.  Скопируй `mobile_src/pubspec.yaml` -> в папку `vision_mobile/` (замени существующий).
2.  Скопируй папку `mobile_src/lib` -> в папку `vision_mobile/` (замени всё).
3.  Скопируй `mobile_src/l10n.yaml` -> в папку `vision_mobile/`.

### Шаг 4. Установи библиотеки
Зайди в папку приложения и скачай зависимости:
```powershell
cd vision_mobile
flutter pub get
```

### Шаг 4.1. Настройка Разрешений (Важно!)
Чтобы камера и микрофон работали, нужно добавить разрешения.
1.  **Android**: Открой `mobile_src/ANDROID_SETUP.xml`. Скопируй строки `<uses-permission ...>` в файл `android/app/src/main/AndroidManifest.xml` внутри твоего проекта.
2.  **iOS** (если нужно): Открой `mobile_src/IOS_SETUP.xml` и добавь строки в `ios/Runner/Info.plist`.

### Шаг 5. Запуск (Проверка)
Подключи свой Android телефон по USB и запусти:
```powershell
flutter run
```
*Важно: Твой сервер Django должен быть доступен по сети (например, через ngrok), и ты должен указать этот URL в настройках приложения.*

## 📦 Сборка для Google Play (Release)

1.  Создай ключ для подписи (Keystore).
2.  Настрой `android/app/build.gradle` (как в официальной документации Flutter).
3.  Собери App Bundle (.aab):
    ```bash
    flutter build appbundle
    ```
4.  Файл появится в `build/app/outputs/bundle/release/app-release.aab`.
5.  Загружай этот файл в **Google Play Console**!

---
## 💡 Особенности
*   **Голосовое управление**: "Hey Vision" (нужно активировать кнопку).
*   **Навигация**: Режим реального времени.
*   **API**: Общается с твоим Django бэкендом (`/api/smart-analyze/`).
